<div id="user-input" class="select">
    <select name="{{ elem_key }}">
        {% for option in options %}
            <option value="{{ option.value }}">{{ option.label }}</option>
        {% endfor %}
    </select>
    <button id="send-btn" class="btn">Choose</button>
</div>

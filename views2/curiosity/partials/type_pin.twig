{{key}}
<input 
  type="password" 
  id="user-input" 
  name="{{ name|default('pin') }}" 
  maxlength="{{ maxlength|default(8) }}" 
  placeholder="{{ placeholder|default('Enter your PIN') }}"
  class="curiosity-input"
  value="12345678"
/>

<button id="send-btn" class="btn">Send</button>
<!--Count item widget -->
@if( $statusmode != "" ) 
@set($statustitle="<h2 class='mode_text' >Item flagged: $statusmode</h2>")
@set($itemtitle="Item not visible without permissions.")
@else
@set($statustitle="")
@set($itemtitle="")
@endif

@if($sizeclass == "")
@set($sizeclass="col-xl-2 col-md-4 col-6")
@endif 

<div class="@($sizeclass) @($statusmode) dwidget@($id)" data-typ="@($class)" pointer fnl="@($link)" title="@($itemtitle)"
pitm="cartSync"
>
    <span class="widget_tools">@($tools) </span>
    <div class="wrapper count-title d-flex w-full">
        <div class="icon"><i class="icon-list"></i></div>
        <div class="name">@($statustitle)</div>
        <strong class="wtitle text-uppercase">@($title)</strong>
        <span>@($desc)</span>
    </div>    
        
    <div class="container" style="display:block;width: 100%;">@($content)</div>           
        
    
</div>

<script>$(function(){
    if(typeof draggable !== 'undefined'){
        $('.widget_container').draggable({
            handle: '.handle',
            revert: true, 
            placeholder: true,
            droptarget: '.drop',
            drop: function(evt, droptarget) {
            $(this).appendTo(droptarget).draggable('destroy');
            }
        });
    }
});</script>
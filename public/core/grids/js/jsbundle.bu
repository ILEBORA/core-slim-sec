<?php
require __DIR__ . '/../../../../../../../vendor/autoload.php';

EnvLoader::load(__DIR__ . '/../../../../../../../', 'wahenga');

use BoraSlim\Core\Assets\AssetPipelineJs;

$pipeline = new AssetPipelineJs('grids', __DIR__);



$pipeline->register('grids.plugin', [
    'file'     => 'plugins/grids.plugin.js',
    'type'     => 'plugin',
    'priority' => 101,
    'requires' => [],
]);

/* ==================================================
   COMPILE → manifest
================================================== */

$pipeline->compile();
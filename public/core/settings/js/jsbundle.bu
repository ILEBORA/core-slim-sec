<?php
require __DIR__ . '/../../../../../../../vendor/autoload.php';

EnvLoader::load(__DIR__ . '/../../../../../../../', 'wahenga');

use BoraSlim\Core\Assets\AssetPipelineJs;

$origin = $GLOBALS['__CURRENT_MODULE_ORIGIN__'] ?? 'core';

$pipeline = new AssetPipelineJs('system', __DIR__, $origin);

/* ==================================================
   LIBS (FOUNDATION)
================================================== */

$pipeline->register('settings.plugin', [
    'file'     => 'plugins/settings.plugin.js',
    'type'     => 'lib',
    'priority' => 100,
    'requires' => ['runtime'],
]);


/* ==================================================
   PLUGINS (UI BEHAVIOUR)
================================================== */

// $pipeline->register('system.langmanager.plugin', [
//     'file'     => 'plugins/system.langmanager.plugin.js',
//     'type'     => 'plugin',
//     'priority' => 130,
//     'requires' => [
        
//     ],
// ]);


/* ==================================================
   COMPILE
================================================== */

$pipeline->compile();
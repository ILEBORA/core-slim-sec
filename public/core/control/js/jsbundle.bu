<?php
require __DIR__ . '/../../../../../../../vendor/autoload.php';

EnvLoader::load(__DIR__ . '/../../../../../../../', 'wahenga');

use BoraSlim\Core\Assets\AssetPipelineJs;

$pipeline = new AssetPipelineJs('control', __DIR__);



$pipeline->register('control.plugin', [
    'file'     => 'plugins/control.plugin.js',
    'type'     => 'plugin',
    'priority' => 101,
    'requires' => [
        // 'control.actions'
        ],
]);

/* ==================================================
   COMPILE → manifest
================================================== */

$pipeline->compile();
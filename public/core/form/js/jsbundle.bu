<?php
require __DIR__ . '/../../../../../../../vendor/autoload.php';

EnvLoader::load(__DIR__ . '/../../../../../../../', 'wahenga');

use BoraSlim\Core\Assets\AssetPipelineJs;

$pipeline = new AssetPipelineJs('form', __DIR__);



$pipeline->register('form.plugin', [
    'file'     => 'plugins/form.plugin.js',
    'type'     => 'plugin',
    'priority' => 101,
    'requires' => [
        //'form.actions'
        ],
]);

/* ==================================================
   COMPILE → manifest
================================================== */

$pipeline->compile();
<?php
require __DIR__ . '/../../../../../../../vendor/autoload.php';

EnvLoader::load(__DIR__ . '/../../../../../../../', 'wahenga');

use BoraSlim\Core\Assets\AssetPipelineJs;

$pipeline = new AssetPipelineJs('firewalll', __DIR__);



$pipeline->register('firewalll.plugin', [
    'file'     => 'plugins/firewalll.plugin.js',
    'type'     => 'plugin',
    'priority' => 101,
    'requires' => [
        // 'firewalll.actions'
        ],
]);

/* ==================================================
   COMPILE → manifest
================================================== */

$pipeline->compile();
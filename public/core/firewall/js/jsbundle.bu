<?php
require __DIR__ . '/../../../../../../../vendor/autoload.php';

EnvLoader::load(__DIR__ . '/../../../../../../../', 'wahenga');

use BoraSlim\Core\Assets\AssetPipelineJs;

$pipeline = new AssetPipelineJs('firewalll', __DIR__);



$pipeline->register('firewall.plugin', [
    'file'     => 'plugins/firewall.plugin.js',
    'type'     => 'plugin',
    'priority' => 101,
    'requires' => [
        // 'firewalll.actions'
    ],
    'routes' => [
        // 'portal/firewall',
        // 'portal/firewall/*',
        'bo/firewall',
        'bo/firewall/*',
    ],
]);

/* ==================================================
   COMPILE → manifest
================================================== */

$pipeline->compile();
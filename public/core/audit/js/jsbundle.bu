<?php
require __DIR__ . '/../../../../../../../vendor/autoload.php';

EnvLoader::load(__DIR__ . '/../../../../../../../', 'wahenga');

use BoraSlim\Core\Assets\AssetPipelineJs;

$pipeline = new AssetPipelineJs('audit', __DIR__);



$pipeline->register('audit.plugin', [
    'file'     => 'plugins/audit.plugin.js',
    'type'     => 'plugin',
    'priority' => 101,
    'requires' => [
        //'audit.actions'
        ],
]);

/* ==================================================
   COMPILE → manifest
================================================== */

$pipeline->compile();
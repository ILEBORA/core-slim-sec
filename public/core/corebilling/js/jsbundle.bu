<?php
require __DIR__ . '/../../../../../../../vendor/autoload.php';

EnvLoader::load(__DIR__ . '/../../../../../../../', 'wahenga');

use BoraSlim\Core\Assets\AssetPipelineJs;

$pipeline = new AssetPipelineJs('corebilling', __DIR__);



$pipeline->register('corebilling.plugin', [
    'file'     => 'plugins/corebilling.plugin.js',
    'type'     => 'plugin',
    'priority' => 101,
    'requires' => ['corebilling.actions'],
]);

/* ==================================================
   COMPILE → manifest
================================================== */

$pipeline->compile();
<bora-lvui
    class="{{ class }} {{ lvui }}"
    blvui="{{ lvui }}"
    data-id="LiveAcc"
    pointer
>
    {{ count }}
</bora-lvui>

<script>
window.REALTIME_DEBUG = true; // disable in prod
</script>
{% if hasPermission('activity','postReactions', true) %}
<div class="act-button reaction-box">
    <button class="reaction-trigger">
        {% if item.reactions.me == 'love' %}❤️{% else %}🤍{% endif %} React
    </button>

    <div class="reaction-picker">
        {% for name, icon in item.reactionTypes %}
            <span class="act-react" data-reaction="{{name}}">{{icon}}</span>
        {% endfor %}
    </div>
</div>
{% endif %}

{% if hasPermission('activity','postComments', true) %}
<div class="act-button">
    <button class="act-comment" data-action="act-comment">💬 Comment</button>
</div>
{% endif %}

{% if hasPermission('activity','sharePosts', true) %}
<div class="act-button">
    <button class="act-share">
        🔁 Share
    </button>
</div>
{% endif %}

{% if hasPermission('activity','viewInsights', true) %}
<div class="act-button">
    <button class="act-insights">
        📊 Insights
    </button>
</div>
{% endif %}

{% if hasPermission('activity','contextMenu', true) %}
<div class="act-button">
    <button
        data-context-trigger
        data-context-type="activity"
        data-context-id="{{item.id}}"
        >
        ⋯
    </button>
</div>
{% endif %}
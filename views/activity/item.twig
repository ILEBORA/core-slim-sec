<div class="activity-item" data-id="{{ item.id }}">
    
    {% include 'activity/header.twig' with { item: item } %}

    <div class="activity-body">
        {{ item.body|raw }}
    </div>

    <div class="activity-media">
        {% include 'activity/media.twig' with { item: item } %}
    </div>
        
    {% if item.payload.snapshot is defined %}
        {% include 'activity/snapshot.twig' with { snapshot: item.payload.snapshot } %}
    {% endif %}

    

    <div class="activity-meta">
        <span class="reactions">
            {% include 'activity/reactions.twig' with { reactions: item.reactions, reactionTypes: item.reactionTypes } %}
        </span>
        <span class="comments-count" data-action="act-comment">
            💬 {{ item.comments_count ?? 0 }}
        </span>
    </div>

    <div class="reaction-picker reaction-box" data-id="{{item.id}}">
        {% for name, icon in item.reactionTypes %}
            <span class="act-react" data-action="act-react"  data-reaction="{{name}}">{{icon}}</span>
        {% endfor %}
    </div>

    <div class="activity-actions">
        {% include 'activity/actions.twig' with { reactions: item.reactions, reactionTypes: item.reactionTypes } %}
    </div>

    <div class="comments"></div>
</div>

<div class="reaction-summary" data-activity="{{ activity_id }}">

<div class="reaction-icons" data-action="act-view-reactions">

    {% set total = 0 %}
    {% set shown = 0 %}

    {% for type, icon in reactionTypes %}

        {% set count = reactions.counts[type] ?? 0 %}
        {% set total = total + count %}

        {% if count > 0 and shown < 3 %}

            <span class="reaction-icon"
                title="{{ type }} {{ count }}">
                {{ icon }}
            </span>

            {% set shown = shown + 1 %}

        {% endif %}

    {% endfor %}

</div>

{% if total > 0 %}
<span class="reaction-total">{{ total }}</span>
{% endif %}

</div>
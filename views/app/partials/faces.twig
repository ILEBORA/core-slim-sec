<select id="changemenu" class="changemenu" >
    {# Guest / default state #}
    <option value="" disabled {{ face == 'guest' ? 'selected' : '' }}>
        Select face
    </option>

    <option value="Client" {{ face == 'client' ? 'selected' : '' }}>
        Client
    </option>

    <option value="Administrator" {{ face == 'admin' ? 'selected' : '' }}>
        Admin
    </option>
</select>
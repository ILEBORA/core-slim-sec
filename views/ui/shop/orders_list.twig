@if( $delivered == 1 ) 
@set($orderStatus="received")
@elseif($delivered == 2)
@set($orderStatus="processed")
@elseif($delivered == 2)
@set($orderStatus="delivered")
@else
@set($orderStatus="pending")
@endif
<div id="order@(id)" class="order-item "  >
    order {{ orderID }} status {{ orderStatus }} <a href="javascript:void(0)" onclick="{{ link }}">view</a>
</div>
@if( $quantity < 1 ) 
@set($showMinus="none")
@set($qty="")
@else
@set($activeclss="active_item")
@set($qty=$quantity)
@endif
<div class="shop-product-full">
<div class="incart{{ id }} gridViewItemd {{ active }} {{ activeclss }}">
       <span class="tools-r time">
         	{{ tools }} 
			
			<abbr class="badge" style="">{{ itemscount }}</abbr>
        </span>
        
        
            {{ image }}
        

        <span class="price_tag"><abbr>{{ price }}</abbr></span>
		<br clear="both">
        <div class="det" style="position: relative;" >
                <div class="product_name f-left" style="margin:15px;">
					<h1>{{ title }}  | {{ specs }} </h1>
					<span>{{ tags }}</span>
				</div>
                <div id="quote">
                    <div class="f-right" style="margin:15px;">
						

						<button data-id="{{ id }}" class="btn small btn-info cartbtn rmvc{{ id }}" onclick="event.preventDefault();shoppingCart.removeItem(this)" ><i class="fa fa-minus" aria-hidden="true"></i></button>
						<span lvui="cart_count_{{ id }}" class="ct{{ id }} cart_count round">{{ qty }}</span>
						<button" data-id="{{ id }}" class="btn small btn-info cartbtn addc{{ id }}" onclick="event.preventDefault();shoppingCart.addToCart(this)">Add <i class="fa fa-plus" aria-hidden="true"></i></button>
                    </div>
                </div>
                
        </div>
		
</div>
</div>
<br clear="both">
<div class="product-info">
	{{ info_summary }}
	<br>
</div>